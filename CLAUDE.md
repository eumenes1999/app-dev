# CLAUDE.md

`04_アプリ開発/` 配下で作業するときのガイド。

## このフォルダについて

- 個人のアプリ開発練習場。gitリポジトリ（ルートの`.gitignore`は`.DS_Store`のみ）。
- 現状はビルドステップなしの素のHTML/CSS/JS（npm・package.jsonなし）。`index.html`をブラウザで直接開ける構成。
- サブフォルダ＝1アプリ1フォルダ（フォルダ名は「アプリ開発 〇〇」）。`開発ナレッジ/`は特定アプリに紐づかない汎用の開発知見置き場。

## 既存アプリ

- `アプリ開発 100日週間トラッカー/` — Supabase使用（`habits`テーブル）
- `アプリ開発 家計簿/` — Supabase使用（`entries`テーブル）
- `アプリ開発 Falloutポートフォリオ/` — バックエンドなし
- `アプリ開発 資格取得/` — バックエンドなし

## Supabase運用ルール（無料枠前提）

無料枠はプロジェクト2つまで、DB 500MB、7日未アクセスで自動一時停止。この制約を前提に、以下のルールで運用する。詳しい経緯は[開発ナレッジ/Supabase無料枠運用ルール_2026-08-01.md](開発ナレッジ/Supabase無料枠運用ルール_2026-08-01.md)を参照。

### 1. 新しいアプリごとに新規プロジェクトを作らない

既存の共有Supabaseプロジェクトにテーブルを追加する形にして、無料枠の2プロジェクト上限を温存する。アプリ間の分離はプロジェクト単位ではなく、**テーブル名＋RLS（`auth.uid() = user_id`）**で行う。

- 現状、100日週間トラッカーと家計簿は同一プロジェクトを共有している（接続情報は各アプリの`app.js`内、`SUPABASE_URL`/`SUPABASE_ANON_KEY`を参照）
- 新規テーブルを追加する際は、他アプリのテーブル名と衝突しない名前にする

別プロジェクトを新規に切ってよいのは、以下のいずれかに該当する場合のみ：

- 用途が大きく異なり、完全な分離が必要な場合（本番相当のクライアントワークなど、練習用途の共有プロジェクトに混ぜたくないもの）
- 共有プロジェクトがDB容量（500MB）や接続数の上限に近づいてきた場合

### 2. スキーマはアプリフォルダ直下に`supabase_schema.sql`として置く

- `CREATE TABLE IF NOT EXISTS` ＋ RLSポリシーを含む、冪等なSQLファイルにする
- Claude CodeからSupabaseへ直接SQLを実行する接続は無い。生成したSQLはユーザーがSupabaseダッシュボードのSQL Editorに貼り付けて手動実行する前提で書く
- ファイル冒頭に日本語コメントで「どのプロジェクトのテーブルか」「実行方法（SQL Editorに貼り付け）」を明記する（既存2ファイルの書式を踏襲）

### 3. プロジェクト・データは消えてよい前提

共有プロジェクトの整理や作り直しが必要になった場合、`supabase_schema.sql`さえgitに残っていれば、新規プロジェクトのSQL Editorに貼り付けるだけで数分でスキーマは復元できる。データ自体（ユーザーの入力内容）は消える前提の練習用途と割り切る。バックアップは無料枠に無いので、残したいデータがある場合は事前に手動エクスポートする。

### 4. APIキーの扱い

Supabaseのanon/publishable keyはRLSで保護される前提の公開鍵なので、`app.js`に直接ハードコードしてよい（既存2アプリと同じ）。service_role keyなど秘匿すべき鍵は、このフォルダのどのファイルにも書かない。
